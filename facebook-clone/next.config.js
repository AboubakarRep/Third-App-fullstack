/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["platform-lookaside.fbsbx.com", "upload.wikimedia.org", "images.pexels.com"]
    }
}

module.exports = nextConfig
